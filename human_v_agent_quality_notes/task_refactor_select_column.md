### The task:
The `select_column` should no longer return a (selected_column, 
potential_columns) tuple. Instead it should modify the passed 
`potential_columns` by removing and returning the selected column.

### The Verdict
- With instructions that specify any implementation detail, and a simple 
refactor, Codex can finish in one shot
