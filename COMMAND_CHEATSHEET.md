### Worktrees:

List: `git worktree list`
Create: `git worktree add ../dls_feature_a -b feature-a`
Remove: `git worktree remove .` (or point to another directory)

### Git (gh):

Create PR: `gh pr create --base base_branch--head head_branch --title "..." --body "..."`
